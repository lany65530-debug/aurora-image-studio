# 自动更新使用说明（GitHub Releases）

本项目已经接好 **electron-updater + GitHub Releases**。你以后只需要「改版本号 + push」，
GitHub 会自动构建、发布安装包，用户端会自动检测并**非全量（差分）**更新。

---

## 一、一次性设置（只做一次）

### 1. 在 GitHub 上建一个空仓库

- 打开 https://github.com/new
- 仓库名建议 `aurora-image-studio`，**必须是 Public（公开）**
- 不要勾选 Add README / .gitignore / license（保持完全空仓库）

### 2. 把项目接到这个仓库

在项目根目录执行（把用户名换成你的）：

```bash
npm run connect:github -- 你的GitHub用户名 aurora-image-studio
```

脚本会自动把 `package.json` 里的 `repository` 和 `build.publish.owner/repo` 写好。

### 3. 首次提交并推送

脚本最后会打印这几条命令，照着执行即可：

```bash
git init
git add -A
git commit -m "chore: 接入 GitHub Releases 自动更新"
git branch -M main
git remote add origin https://github.com/你的GitHub用户名/aurora-image-studio.git
git push -u origin main
```

> 推送时如果要求登录，用 GitHub 的 **Personal Access Token** 当密码
> （GitHub → Settings → Developer settings → Personal access tokens → 勾 `repo`）。
> 或者安装 GitHub Desktop / 使用已登录的 Git 凭据管理器。

### 4. 给 Actions 写权限

仓库 → **Settings → Actions → General** → 拉到最下面
**Workflow permissions** 选 **Read and write permissions** → Save。

（`secrets.GITHUB_TOKEN` 是 GitHub 自动提供的，不需要你手动创建任何密钥。）

---

## 二、以后每次发新版（3 条命令）

```bash
npm version patch      # 自动把版本号 +1，并打上 v 开头的 tag
git push               # 推送代码
git push --tags        # 推送 tag
```

推送 tag 后，GitHub Actions 会自动：

1. 安装依赖 → 构建 → 打包 NSIS 安装包
2. 生成 `AuroraStudio-Setup-x.y.z.exe.blockmap`（差分下载用）
3. 生成 `latest.yml`（更新清单）
4. 创建 Release 并上传以上全部文件

进度在仓库的 **Actions** 页面看。完成后在 **Releases** 页面能看到新版本。

---

## 三、版本号规则（重要）

| 规则 | 说明 |
| --- | --- |
| 从 **1.0.1** 开始 | 公开发布的第一个版本就是 `v1.0.1` |
| 只能递增 | `1.0.1 → 1.0.2 → 1.0.3 → … → 1.0.10`，**绝不能低于线上已有版本** |
| 用 `npm version` 改 | 不要手改 `package.json` 的 version，`npm version patch` 会同时改版本 + 建 tag |
| tag 与版本号一致 | tag 必须是 `v1.0.2` 这种格式，electron-builder 靠它找上一版做差分 |

> 为什么不能降版本？客户端只认「比我现在更高」的版本，低版本会被忽略（防止被恶意降级）。

---

## 四、用户端会发生什么

- 应用**启动约 8 秒后**自动静默检查一次更新，之后**每 6 小时**检查一次
- 标题栏右上角有一个 **「检查更新」** 胶囊按钮，用户也可以手动点
- 发现新版本 → 弹窗显示版本号与更新说明 → 点「立即下载」
- 下载完按钮变成 **「重启以更新」** → 点一下，应用自动重启并安装
- **非全量下载**：只下载新旧安装包不同的块（差分），失败会自动退回整包下载，不用担心更新失败
- 开发模式（`npm run dev`）不会检查更新，这是正常的

---

## 五、注意事项与排错

| 现象 | 原因 / 处理 |
| --- | --- |
| 一直提示「更新失败：未找到发布信息」 | 仓库是私有仓库，或还没发过 Release。必须是 Public 且已发布 |
| 第一次更新就是整包下载 | 正常。首次没有更旧的版本可对比，1.0.1 → 1.0.2 起才会走差分 |
| 差分没生效 | 别删除旧 Release —— 差分需要上一版同名的 `.blockmap` |
| 新版本发出了但客户端没反应 | 客户端 6 小时才自动查一次；让用户点标题栏「检查更新」即可 |
| 安装时 Windows 弹 SmartScreen 警告 | 安装包没有代码签名（要买证书），点「仍要运行」即可。不影响更新功能 |
| Actions 报权限错误 | 回到「一次性设置」第 4 步，确认 Workflow permissions 是 Read and write |

---

## 六、本地打包（不发布）

```bash
npm run dist:win      # 只打安装包，产物在 release/ 目录
npm run pack:win      # 只解包不生成安装程序，便于调试
```

`release/` 已在 `.gitignore` 中，不会被提交。

---

## 七、本次改动清单（备查）

| 文件 | 作用 |
| --- | --- |
| `package.json` | 版本改为 1.0.1；新增 `electron-updater` 依赖；配置 `build.publish`（GitHub）与 `artifactName`（ASCII 文件名）；新增 `release:win`、`connect:github` 脚本 |
| `src/main/services/updater.ts` | 新增。更新服务：检查 / 下载 / 重启安装、状态机、中文错误提示、自动回退整包 |
| `src/main/controllers/updater.ts` | 新增。更新相关 IPC |
| `src/main/index.ts` | 注册更新 IPC 与启动更新服务 |
| `src/shared/types.ts` | 新增 `UpdaterStatus` / `UpdaterState` 类型 |
| `src/shared/ipc.ts` | 新增 `Updater` 通道与 `updaterStatus` 事件 |
| `src/shared/aurora-api.ts` | 新增 `updater` API 契约 |
| `src/preload/api.ts` | 暴露 `window.aurora.updater` |
| `src/renderer/src/components/updateBadge.ts` | 新增。标题栏更新按钮逻辑 |
| `src/renderer/src/styles/component/updateBadge.css` | 新增。按钮样式 |
| `src/renderer/index.html` | 标题栏加入更新按钮 + 引入样式 |
| `src/renderer/src/state/aurora.ts` | 导出 `updater` |
| `src/renderer/src/main.ts` | 挂载更新按钮 |
| `.github/workflows/release.yml` | 新增。打 tag 自动构建并发布到 Release |
| `scripts/connect-github.cjs` | 新增。一键写入仓库配置 |
